{
  "name": "docker-android",
  "version": "0.0.0",
  "description": "Generates Dockerfiles with different versions of Android build tools and platform tools",
  "main": "index.js",
  "repository": "git@github.com:rulych/docker-android.git",
  "author": "Rubén Illodo Brea <me@rubenillodo.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node index",
    "format": "prettier --write '**/*.{js,json}'",
    "clean": "rimraf build"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "cross-fetch": "^2.1.0",
    "fs-extra": "^5.0.0"
  },
  "devDependencies": {
    "prettier": "^1.11.1",
    "rimraf": "^2.6.2"
  }
}
